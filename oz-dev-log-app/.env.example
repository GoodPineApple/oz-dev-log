# API origin (프로토콜 + 호스트 + 포트, 끝 슬래시 없음)
# - 비워 두기(권장, 로컬): `/users` 등 상대 경로 → Vite 프록시 → CORS 이슈 없음, 포트(5173/5175 등) 무관
# - 채우기: 예) http://localhost:3000 — 브라우저가 직접 호출(백엔드가 해당 Origin CORS 허용 필요)
VITE_API_BASE_URL=
